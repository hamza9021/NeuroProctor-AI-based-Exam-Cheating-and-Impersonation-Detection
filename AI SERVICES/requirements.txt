# =============================================================================
# NeuroProctor FastAPI AI Backend — Python Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
#
# GPU Note: onnxruntime-gpu requires a compatible CUDA installation.
# If no GPU is available, ONNX Runtime will fall back to CPUExecutionProvider.
# To force CPU-only, replace onnxruntime-gpu with onnxruntime.
# =============================================================================

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9          # Required for multipart/form-data (file uploads)

# ── JWT Authentication ────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0 # JWT decode/verify (python-jose with RSA/HMAC)

# ── MongoDB Async Driver ──────────────────────────────────────────────────────
motor>=3.4.0                     # Async Motor driver wrapping PyMongo
pymongo>=4.7.0                   # PyMongo (required by Motor for BSON, etc.)

# ── Cloudinary ────────────────────────────────────────────────────────────────
cloudinary>=1.40.0               # Official Cloudinary Python SDK

# ── InsightFace + GPU Runtime ─────────────────────────────────────────────────
insightface>=0.7.3               # ArcFace model: buffalo_l (512-dim embeddings)
onnxruntime-gpu>=1.18.0          # ONNX GPU runtime (CUDA). See note above.

# ── Image Processing ──────────────────────────────────────────────────────────
opencv-python-headless>=4.9.0    # OpenCV without GUI (server-safe)
numpy>=1.26.0                    # Numerical arrays for embeddings
Pillow>=10.3.0                   # Image decoding utility

# ── Configuration & Validation ────────────────────────────────────────────────
pydantic[email]>=2.7.0           # Data validation (v2) + EmailStr support
pydantic-settings>=2.3.0         # BaseSettings for environment variable loading
email-validator>=2.0.0           # Required by pydantic[email] for EmailStr fields
python-dotenv>=1.0.0             # Load .env files

# ── HTTP Client ────────────────────────────────────────────────────────────────
httpx>=0.27.0                    # Async HTTP client for Express backend communication

# ── Real-Time Communication ───────────────────────────────────────────────────────
python-socketio[asyncio]>=5.11.0  # Socket.IO for real-time event broadcasting
aiohttp>=3.9.0                    # Async HTTP client (required by python-socketio)

# ── YOLO Object Detection ───────────────────────────────────────────────────────────
ultralytics>=8.1.0                # YOLOv8 object detection library
torch>=2.1.0                      # PyTorch (required by ultralytics)
torchvision>=0.16.0               # Torch vision utilities (required by ultralytics)

# ── DeepSORT Multi-Object Tracking ─────────────────────────────────────────────────
# Using custom centroid tracker (no external DeepSORT dependency)
scikit-learn>=1.3.0               # Required for centroid distance calculations
scipy>=1.11.0                     # Required for centroid distance calculations

# ── Head Pose Estimation ─────────────────────────────────────────────────────────────
# sixdrepnet provides the official SixDRepNet architecture and Euler-angle utilities
# used by HeadPoseModelLoader (loader.py) and HeadPoseParser (parser.py).
# Requires Python >=3.10, PyTorch >=2.1 (already listed above).
# Local checkpoint: models/6drepnet/6DRepNet_300W_LP_AFLW2000.pth
# Automatic weight downloading is disabled; the .pth file must be present locally.
sixdrepnet>=0.1.1                 # Official SixDRepNet head-pose estimation package


# ── Testing ───────────────────────────────────────────────────────────────────────
pytest>=8.0.0                     # Unit testing framework
pytest-asyncio>=0.21.0            # Async support for pytest
