# AI Services - Video Processing Pipeline Requirements

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0

# Progress bar
tqdm>=4.65.0

# Type hints (optional but recommended)
typing-extensions>=4.5.0
setuptools>=60.0.0,<70.0.0

# YOLO Object Detection (Phase 2)
ultralytics>=8.0.0
# PyTorch versions compatible with sixdrepnet 0.1.6
# For GPU support: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# For CPU-only: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<2.2.0
torchvision>=0.15.0,<0.17.0

# Multi-Object Tracking (Phase 6)
# Using DeepSORT for robust multi-object tracking with re-identification
deep-sort-realtime>=1.3.0

# Head Pose Estimation (Phase 7)
# Using 6DRepNet for accurate head orientation estimation
# IMPORTANT: 6DRepNet requires specific PyTorch versions for compatibility
# Install sixdrepnet: pip install sixdrepnet==0.1.6
# If sixdrepnet is not installed, the head pose processor will be skipped automatically
sixdrepnet==0.1.6

# Future AI model dependencies (for future phases)
# transformers>=4.30.0
# timm>=0.9.0
# scikit-learn>=1.3.0
